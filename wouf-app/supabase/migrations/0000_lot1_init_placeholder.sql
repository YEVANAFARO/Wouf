-- LOT 1 placeholder migration
-- Purpose: initialize versioned migration flow without changing runtime schema yet.
-- NOTE: V2 schema migration is intentionally deferred to LOT 2.

-- no-op
SELECT 1;
